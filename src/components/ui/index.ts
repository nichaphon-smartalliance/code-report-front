/**
 * Project-default wrappers over Mantine primitives (SPEC-002 Decision 2,
 * deviation 1: a wrapper exists only where the project applies a real default).
 *
 * Deliberately empty after TASK-010 — this TASK introduces no Mantine
 * component, so no project default exists yet to hold. TASK-011 onwards fill it.
 */
export {};
